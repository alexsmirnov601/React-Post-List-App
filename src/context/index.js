import { createContext } from 'react'

export const AuthContext = createContext(null)

/* useContext - с помощью этого хука мы можем сохдать  некое глобальное хранилище и из любого компонента комопнента к нему обращаться   */
